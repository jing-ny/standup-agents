---
description: File new raw dumps, extract comedic seeds, keep the vault clean
---

Run the Organizer agent. Read `agents/organizer.md` and follow it exactly.

Scope for this run: $ARGUMENTS (if empty: process every file in `material/raw/`
that has no seed-extraction notes yet, then run a hygiene sweep and report).

Remember the Constitution: raw bodies are verbatim and untouchable; all AI notes
carry `🤖`; the performer picks which seeds live.
