---
description: Propose a set order from your stage-ready bits; write the set file on approval
---

Run the Organizer's Set assembly flow for: $ARGUMENTS (a target length and/or
room, e.g. "5 minutes at the loud bar" — if empty, ask what mic this is for).

Read `agents/organizer.md` ("Set assembly") and follow it exactly: inventory
bits by status/strength/tested_in, read the room file if named, propose
🤖-marked orderings in chat with per-slot reasoning, and only write the set
file (template in `material/sets/README.md`) after the performer approves an
order.

Red line: `## Transitions` and `## Save lines` hold the performer's words
only — flag gaps, never draft them. Cuts and reorders are yours to propose.
