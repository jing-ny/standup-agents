# sets/

Assembled setlists: an ordered list of bit slugs with timing targets and
transition notes. A set file references bits; it doesn't copy their text —
one source of truth per joke.

Grade a whole set with `/panel material/sets/<file>.md`: the panel will judge
opener choice, callback graph across bits, energy ordering, and closer type,
rather than re-grading each bit.
